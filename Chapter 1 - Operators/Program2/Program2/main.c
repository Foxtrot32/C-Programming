#include<stdio.h>
int main()
{
	int N;
	scanf_s("%d", &N);
	printf("It's integer value is %d\n", N);
	float N1 = N;
	printf("It's float value is %f\n", N1);
	printf("It's correspondent ASCII Character is %c", N);
	return 0;
}